# redis-simple
Simple redis in C++.

## Building
```sh
mkdir build && cd build
cmake .. && cmake --build .
```

## Running Unit Tests
```sh
cd build && ./memory_test
```

### Running Benchmark
```sh
cd build && ./memory_benchmark
```
