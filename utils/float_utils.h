#pragma once

#include <stdlib.h>

#include <string>
#include <vector>

namespace redis_simple {
namespace utils {
// Convert floating number to string.
std::string FloatToString(double fl);
}  // namespace utils
}  // namespace redis_simple
